{
  "name": "@not-not-found/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@not-not-found/shared": "workspace:*"
  }
}
