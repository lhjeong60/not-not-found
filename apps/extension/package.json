{
  "name": "@not-not-found/extension",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@not-not-found/shared": "workspace:*"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "2.0.0-beta.33",
    "@types/chrome": "^0.1.38",
    "typescript": "^5.7.0",
    "vite": "^8.0.0"
  }
}
